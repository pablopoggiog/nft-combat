import styled from "styled-components";

const App = () => {
  return <Container>Hey there, this is my own empty (for now) React app for the Buildspace COURSE (kidding, "hackathon" 😜 lol), deployed through GH Actions to GH Pages</Container>;
};

export default App;

const Container = styled.div`
  text-align: center;
  padding: 1em;
`;
